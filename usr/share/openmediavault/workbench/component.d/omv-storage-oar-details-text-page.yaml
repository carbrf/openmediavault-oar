version: "1.0"
type: component
data:
  name: omv-storage-oar-details-text-page
  type: textPage
  config:
    hasReloadButton: true
    request:
      service: Oar
      get:
        method: getDetail
        params:
          name: '{{ _routeParams.name }}'
